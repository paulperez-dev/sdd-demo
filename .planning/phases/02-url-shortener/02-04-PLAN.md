---
phase: 02-url-shortener
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - lib/main.dart
  - lib/app.dart
autonomous: false
requirements:
  - SHORT-03
  - SHORT-05

must_haves:
  truths:
    - "User can paste a URL, click Shorten, and see the short URL displayed in the app"
    - "Opening the short URL in a browser redirects to the original URL with HTTP 302"
    - "Visiting a non-existent short URL in a browser returns a plain-text 404 response"
    - "Short URLs created in one session still redirect correctly after restarting the app"
    - "The app window shows ShortenerScreen (not FoundationScreen)"
  artifacts:
    - path: "lib/main.dart"
      provides: "Updated main() passing database to ServerController constructor"
      contains: "ServerController(database: database)"
    - path: "lib/app.dart"
      provides: "Updated App widget using ShortenerScreen as home"
      contains: "ShortenerScreen"
  key_links:
    - from: "lib/main.dart"
      to: "lib/server/server_controller.dart"
      via: "ServerController(database: database)"
      pattern: "ServerController\\(database:"
    - from: "lib/app.dart"
      to: "lib/ui/shortener_screen.dart"
      via: "home: ShortenerScreen(serverController: ...)"
      pattern: "ShortenerScreen"
---

<objective>
Wire Phase 2 components into the app entry point: pass AppDatabase to ServerController, and replace FoundationScreen with ShortenerScreen as the app home. Then perform human verification of the complete end-to-end flow.

Purpose: This plan closes the loop — all pieces built in Plans 01-03 are connected. After this plan, the complete v1 URL shortener works: paste URL → get short URL → browser redirects. Also verifies SHORT-03 and SHORT-05 (persistence).

Output: Updated main.dart + app.dart, plus human confirmation of the full redirect loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/main.dart
@lib/app.dart
@.planning/phases/02-url-shortener/02-02-SUMMARY.md
@.planning/phases/02-url-shortener/02-03-SUMMARY.md

<interfaces>
<!-- Updated ServerController constructor (from 02-02) -->
```dart
ServerController({required AppDatabase database})
```

<!-- ShortenerScreen constructor (from 02-03) -->
```dart
ShortenerScreen({super.key, required ServerController serverController})
```

<!-- Current main.dart creates ServerController() with no args — must change to: -->
```dart
final serverController = ServerController(database: database);
```

<!-- Current app.dart home is FoundationScreen — must change to ShortenerScreen -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main.dart and app.dart to wire Phase 2 components</name>
  <files>lib/main.dart, lib/app.dart</files>
  <read_first>
    - lib/main.dart (current — ServerController() has no args; must add database: database)
    - lib/app.dart (current — home: FoundationScreen(...); must replace with ShortenerScreen)
    - lib/server/server_controller.dart (confirm new constructor signature: ServerController({required AppDatabase database}))
    - lib/ui/shortener_screen.dart (confirm constructor: ShortenerScreen({required ServerController serverController}))
  </read_first>
  <action>
**Update lib/main.dart:**

Change the ServerController instantiation to pass the database parameter.

Current line to replace:
```dart
final serverController = ServerController();
```

New line:
```dart
final serverController = ServerController(database: database);
```

No other changes to main.dart.

**Update lib/app.dart:**

1. Replace the import of foundation_screen.dart with shortener_screen.dart:
   - Remove: `import 'ui/foundation_screen.dart';`
   - Add: `import 'ui/shortener_screen.dart';`

2. Replace the home widget:
   - Remove: `home: FoundationScreen(serverController: widget.serverController),`
   - Add: `home: ShortenerScreen(serverController: widget.serverController),`

No other changes to app.dart. The AppLifecycleListener and database passing logic must remain intact.

After edits, run:
```
flutter analyze lib/
```
Both files must analyze clean.
  </action>
  <verify>
    <automated>cd /Users/paulperez/development/sdd-demo && flutter analyze lib/</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "ServerController(database: database)" lib/main.dart returns a match
    - grep -n "ShortenerScreen" lib/app.dart returns a match
    - grep -n "foundation_screen" lib/app.dart returns no output (import removed)
    - grep -n "FoundationScreen" lib/app.dart returns no output (widget replaced)
    - flutter analyze lib/ reports "No issues found"
  </acceptance_criteria>
  <done>main.dart passes database to ServerController; app.dart uses ShortenerScreen as home; flutter analyze clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 2 end-to-end URL shortening flow</name>
  <action>Human verifies the complete Phase 2 URL shortening loop end-to-end as described in how-to-verify below. No automated action — this is a human verification step.</action>
  <verify>Human confirms all listed checks pass by typing "approved"</verify>
  <done>User types "approved" confirming: short URL generated in UI, browser 302 redirect works, 404 for unknown slugs, and URLs persist across app restart</done>
  <what-built>
Complete Phase 2 URL shortener:
- SlugGenerator (base62 from row ID)
- POST /shorten handler (URL validation + DB insert + slug generation)
- GET /[slug] handler (302 redirect or 404)
- ShortenerScreen UI (text input, Shorten button, short URL display with copy)
- App wired end-to-end: database → ServerController → ShortenerScreen
  </what-built>
  <how-to-verify>
1. Run the app: `flutter run -d macos` (or your platform)
2. Confirm the window opens showing ShortenerScreen (text input + Shorten button, NOT the old FoundationScreen card)
3. Confirm the server status badge shows green dot + "Server Running" + http://localhost:PORT

**Test: shorten a URL**
4. Paste `https://flutter.dev` into the input field and click Shorten
5. Confirm a short URL appears in the result area (format: `http://localhost:PORT/XXXXXX`)
6. Confirm the short URL is exactly 6 characters after the last slash (e.g. `000001`)

**Test: browser redirect**
7. Copy the short URL and open it in your browser (or click the copy button then paste)
8. Confirm the browser redirects to https://flutter.dev
9. In browser DevTools (Network tab), confirm the redirect status was 302 (not 301)

**Test: 404 for unknown slug**
10. Open http://localhost:PORT/xxxxxx in browser (PORT = actual bound port)
11. Confirm you get a plain-text "Not found" response (not a browser connection error)

**Test: persistence (SHORT-03 + SHORT-05)**
12. Close the app
13. Reopen the app with `flutter run -d macos`
14. Open the short URL from step 5 in the browser
15. Confirm it still redirects to https://flutter.dev (slug persisted in SQLite)

**Test: URL validation**
16. Paste `javascript:alert(1)` into the input and click Shorten
17. Confirm an error message appears (not a short URL)

Report any failures with the exact error message or unexpected behavior.
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe specific failures</resume-signal>
</task>

</tasks>

<verification>
- flutter analyze lib/ reports "No issues found"
- App shows ShortenerScreen on launch
- POST /shorten → short URL displayed → browser 302 redirect → original URL
- 404 for unknown slugs
- URLs persist across app restarts
</verification>

<success_criteria>
All four roadmap success criteria for Phase 2 confirmed by user:
1. User pastes URL, app displays short URL (http://localhost:PORT/SLUG)
2. Short URL in browser → HTTP 302 → original URL
3. Short URLs work after app restart
4. Unknown short URL → 404 in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-url-shortener/02-04-SUMMARY.md`
</output>
